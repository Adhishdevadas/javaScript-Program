var showMessage;

showMessage = function (message) {
	console.log("The message is: " + message);
};

showMessage("It's full of stars!");

// Further Adventures

showMessage("The galaxy awaits!");
showMessage("Space is vast and mysterious.");
showMessage("Explore the cosmos.");

// Changing the showMessage function

showMessage = function (message) {
	console.log("The message is: " + message + ". Remember to look up!");
};

showMessage("Look at the stars.");
